source:
  repo:
    dockerfile: SnowIndexComputation/Dockerfile
    password: repo_password
    url: https://gitlab.com/wds-co/SnowWatch-SODALITE.git
    username: repo_username
    version: docker_images
    workdir: SnowIndexComputation
  type: git
target:
  image_name: snow-index-computation
  image_tag: latest
  registry_ip: localhost:5000
